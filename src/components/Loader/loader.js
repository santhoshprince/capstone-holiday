import React from 'react';
import './loader.css'; // Import the CSS for the loader // Adjust the path as necessary

const Loader = () => {
  return (
    <div class="global-loader">
    <i class="fas fa-plane loader-icon"></i>
    {/* <div class="loader-text">Loading Package Details...</div> */}
  </div>
  
  );
};

export default Loader;
